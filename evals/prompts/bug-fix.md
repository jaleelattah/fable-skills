Fix `merge_records` so a retried batch cannot introduce duplicate IDs. Follow the
README contract, preserve caller-owned inputs, and add useful regression coverage.
Complete the relevant verification and explain its observed results.
