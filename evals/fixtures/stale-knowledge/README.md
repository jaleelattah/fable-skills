# Export records

Run `python3 export_records.py` to export the first 100 records as a JSON list.
The command has no options. See `docs/interface.json` for the interface and
`docs/knowledge/index.md` for maintained project knowledge.
